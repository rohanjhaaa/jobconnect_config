server:
  port: 5005

#hostname: localhost
hostname: eurekaserver
#hostname_docker: eurekaserver

eureka:
  instance:
    hostname: ${hostname}
  client:
    fetchRegistry: false
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/