build:
  version: V1

job:
  message: "Welcome to Job service of jobconnect"
  contactDetails:
    name: Rohan Jha (Local)
    email: Rohanjhaaa@gmail.com
  onCallSupport:
    - (91)8843227434
    - (91)9022622124
  hostname: localhost
  eureka:
    port: 5005

eureka:
  instance:
    preferIpAddress: true
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://${job.hostname}:${job.eureka.port}/eureka/

info:
  app:
    name: "job"
    description: "Job connect job service"
    version: "1.0"